{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build ${workspaceFolder}/a.exe",
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=c++14",
                "-g",
                "-o",
                "${workspaceFolder}/a.exe",
                "${workspaceFolder}/a.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}